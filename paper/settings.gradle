plugins {
    // Lets Gradle auto-provision the JDK toolchain (Java 25 for Paper 26.2).
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'gatekeeper-paper'
