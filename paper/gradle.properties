# Paper API version to compile against. Matches the running server (Paper 26.2,
# which targets Java 25 - see the Java toolchain in build.gradle). Bump this to your
# exact server build if you upgrade Paper.
paperApiVersion=26.2.build.25-alpha
