# Paper API version to compile against. The plugin only uses stable Bukkit API,
# so a recent release is fine even when running on a much newer server (e.g. 26.x).
# Bump this if you prefer compiling against your exact server version.
paperApiVersion=1.21.4-R0.1-SNAPSHOT
